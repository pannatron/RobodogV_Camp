# ฟังก์ชันสำหรับแปลงอุณหภูมิ
def convert_temperature(mode, temperature):
    # เช็ค mode และคำนวณอุณหภูมิตามสูตร
    if mode == "C -> F":
       "เติมโปรแกรมทีนี่"
    elif mode == "F -> C":
       "เติมโปรแกรมทีนี่"

# ตัวอย่างการใช้งานฟังก์ชัน
print(convert_temperature("C -> F", 100)) # ควรแสดงผลลัพธ์: 212.0 Fahrenheit
print(convert_temperature("F -> C", 32)) # ควรแสดงผลลัพธ์: 0.0 Celsius
